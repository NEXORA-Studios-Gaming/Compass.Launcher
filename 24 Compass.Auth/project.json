{
  "name": "compass-auth",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "24 Compass.Auth/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build --release",
        "cwd": "24 Compass.Auth"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test",
        "cwd": "24 Compass.Auth"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo clippy",
        "cwd": "24 Compass.Auth"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo check",
        "cwd": "24 Compass.Auth"
      }
    },
    "fmt": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo fmt",
        "cwd": "24 Compass.Auth"
      }
    }
  },
  "tags": ["lib", "rust"],
  "implicitDependencies": ["compass-core"]
}
